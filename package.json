{
  "name": "litemetrics",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "clean": "turbo clean",
    "typecheck": "turbo typecheck",
    "release": "turbo build --force && cd packages/core && npm publish --access public && cd ../tracker && npm publish --access public && cd ../client && npm publish --access public && cd ../node && npm publish --access public && cd ../react && npm publish --access public && cd ../react-native && npm publish --access public"
  },
  "packageManager": "bun@1.2.1",
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5.7"
  }
}
