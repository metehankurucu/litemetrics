{
  "name": "litemetrics",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "clean": "turbo clean",
    "typecheck": "turbo typecheck",
    "release": "turbo build --force && cd packages/core && bun publish --access public && cd ../tracker && bun publish --access public && cd ../client && bun publish --access public && cd ../node && bun publish --access public && cd ../react && bun publish --access public && cd ../react-native && bun publish --access public"
  },
  "packageManager": "bun@1.2.1",
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5.7"
  }
}
